# my-reports 项目指南

Before answering or editing files, read and follow:

/Users/kanehua/.config/.agents/AGENTS.md

The global file controls interaction style.
This project file controls repository-specific behavior.

## 项目概述

这是一个**个人报告聚合站点**，通过 GitHub Pages 部署。核心功能是将 `reports/` 目录下的 HTML 报告自动聚合并生成索引页。

- **站点入口**：`index.html`（由脚本自动生成）
- **部署地址**：GitHub Pages（push 到 `main`/`master` 分支自动触发）
- **构建命令**：`npm run build` 或 `node generate-index.js`

---

## 目录结构

```
my-reports/
├── index.html              # 自动生成的报告索引页（不要手动编辑）
├── generate-index.js       # 索引生成脚本（核心逻辑）
├── package.json            # 仅包含 build 脚本
├── reports/                # 报告存放目录
│   ├── *.html              # 独立 HTML 报告文件
│   ├── case/               # 子目录形式的多页报告
│   │   └── index.html      # 每个子目录必须包含 index.html
│   ├── april-report/
│   ├── bigthing/
│   ├── mac/
│   └── InvestmentThesisPortfolioPlaybook/
├── .github/workflows/      # GitHub Actions 部署配置
└── .sisyphus/              # Sisyphus 任务计划追踪（勿动）
    ├── boulder.json
    ├── drafts/
    ├── evidence/
    └── plans/
```

---

## 核心逻辑：`generate-index.js`

### 扫描规则

1. **独立文件**：`reports/` 下直接的 `.html` 文件，按文件名倒序排列（最新的在前）
2. **子目录**：`reports/` 下的文件夹，如果包含 `index.html` 则收录，按目录名正序排列
3. **排序**：子目录排在独立文件前面；同类型按规则排序

### 配置项

```js
const titleAliases = {
  case: "2026-3投资战略备忘录",  // 目录别名，覆盖从 index.html 读取的标题
};

const excludedDirs = new Set(["assets", "shared", "lib"]);  // 忽略的目录
```

### 标题优先级

子目录的显示标题按以下优先级：
1. `titleAliases[name]` —— 手动配置的别名
2. `index.html` 中的 `<title>` 标签内容
3. 目录名本身

---

## 如何添加新报告

### 方式一：单页报告（推荐简单报告）

直接将 `.html` 文件放入 `reports/` 目录：

```bash
reports/2026XX-报告标题.html
```

文件名会成为索引页的链接标题（去掉 `.html` 后缀）。

### 方式二：多页报告站点（推荐复杂报告）

在 `reports/` 下创建子目录，目录内必须包含 `index.html`：

```bash
reports/my-report/
├── index.html      # 入口页（必须有 <title> 标签）
├── page2.html
└── assets/
    └── style.css
```

如需自定义索引页显示名称，在 `generate-index.js` 的 `titleAliases` 中添加映射。

### 方式三：排除某些目录

如果目录不需要出现在索引中（如共享资源目录），添加到 `excludedDirs` 中。

---

## 构建与部署

### 本地构建

```bash
npm run build
# 或
node generate-index.js
```

执行后会重新生成根目录的 `index.html`。

### 自动部署

项目配置了 GitHub Actions（`.github/workflows/deploy.yml`）：
- 触发条件：push 到 `main` 或 `master` 分支
- 部署目标：GitHub Pages
- **不需要手动构建后提交**——直接 push 源码即可，GitHub Actions 会自动部署整个仓库内容

> 注意：`index.html` 是构建产物。如果 CI 环境中没有运行 `generate-index.js`，则需要确保提交前本地运行过构建，或者将构建步骤加入 CI。

当前 CI **没有**运行构建步骤，因此 **push 前请确保已执行 `npm run build` 并提交最新的 `index.html`**。

---

## 编码规范

- `generate-index.js` 使用 Node.js 原生 API（`fs`, `path`），无外部依赖
- HTML 报告使用中文（`zh-CN`），确保包含 `<meta charset="utf-8">`
- 子目录报告的 `index.html` 应包含 `<title>` 标签以便正确显示标题
- 不要手动编辑根目录的 `index.html`，它会被脚本覆盖

---

## 注意事项

1. **目录名约定**：以 `.` 或 `_` 开头的文件/目录会被自动忽略。
2. **大小写敏感**：GitHub Pages 和本地文件系统对大小写敏感度不同，目录名和文件名建议统一使用小写或驼峰。
3. **防呆设计**：`generate-index.js` 会在 `reports/` 目录不存在时自动创建，避免在 Vercel 等环境报错。

---

## 依赖

- Node.js（任意现代版本即可，仅使用原生模块）
- 无 npm 包依赖

---

## Project Coding Rules

### Tech Stack

- Runtime: Node.js
- Framework: 无（原生脚本）
- Package manager: npm

### Commands

- Install: `npm install`（无实际依赖，可跳过）
- Dev: not available
- Test: not available
- Typecheck: not available
- Lint: not available
- Build: `npm run build`

### Verification

- After changing runtime behavior, run `npm run build` and verify the generated `index.html`.
- After changing frontend UI (index.html template), open the generated `index.html` in browser to verify layout and links.
- If a check is skipped, report it explicitly.

### Boundaries

- Do not edit `.sisyphus/` and its contents unless explicitly asked.
- Do not modify `index.html` by hand; it is generated by `generate-index.js`.
- Do not change `.github/workflows/` unless the task specifically requires it.
- Preserve existing user changes in `reports/`.

### Local Conventions

- Follow existing module layout: `generate-index.js` at project root, reports under `reports/`.
- Use Node.js native `fs`/`path` APIs before adding new npm dependencies.
- Match nearby code style in `generate-index.js`.
- Keep changes scoped to the request.
